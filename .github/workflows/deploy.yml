name: Deploy Zentrix Web

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy na VM Ubuntu
        run: /usr/local/bin/deploy-zentrix-web.sh
